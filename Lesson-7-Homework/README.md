# Homework7

* 实现一个非阻塞 fibonacci 函数，在运行时，页面可以正常响应用户操作

* Node.js 实现一个 static server，支持 cache=max-age | no-cache | no-store，来控 制返回的文件的 cache-control

  运行方式：

```js
npm install
node server.js
打开http://localhost:3000/
```

